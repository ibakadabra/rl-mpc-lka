# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# TensorBoard / SB3 monitor — regeneratable, can be large
logs/

# Large model checkpoints (CleanRL .pt files); the SB3 sac_rlmpc.zip + the
# vecnormalize.pkl ARE committed so the remote validation PC has the model
# right after `git pull`.
models/*.pt
models/*.bin

# CARLA install dir (only on machines where the simulator lives)
CarlaUE4/

# OS noise
.DS_Store
Thumbs.db

# Bulk data files kept outside the repo
*.hdf5

# Smoke / scratch CSVs we don't want to ship; the canonical results stay tracked.
results/eval_smoke.csv
